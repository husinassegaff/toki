#include <stdio.h>


int main(){
    int N;

    cin >> N;

    int A[N-1];
    int B[N-1];

    for (int i = 0; i <= N; ++i){
        cin >> A[i];
    }

     for (int i = 0; i <= N; ++i){
        cin >> B[i];
    }
}